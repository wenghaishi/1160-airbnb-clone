<div class="max-w-md mx-auto bg-white shadow-xl mt-2 rounded-xl border">
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <div class="flex items-center justify-between px-6 border-b py-5">
      <p><a href="<%= root_path %>"><i class="fa-regular fa-less-than"></i></a></p>
      <h2><b>Sign up</b></h2>
      <p></p>
    </div>
    <div class="p-6 pb-3">
      <div class="mb-3 sign_up">
        <%= f.input :first_name,
                  required: true,
                  label: false,
                  autofocus: true,
                  input_html: { autocomplete: "first_name", placeholder: "First name", class: "rounded-lg border z-0 w-full py-2 px-3 text-gray-700 focus:outline-none focus:shadow-outline m-0"}%>
        <%= f.input :last_name,
                  required: true,
                  label: false,
                  autofocus: true,
                  input_html: { autocomplete: "last_name", placeholder: "Last name", class: "rounded-lg border z-0 w-full py-2 px-3 text-gray-700 focus:outline-none focus:shadow-outline m-0" }%>
      </div>
      <p class="text-xs text-gray-400 mb-5">Make sure it matches the name on your government ID.</p>
      <div class="sign_up">
        <%= f.input :email,
              required: true,
              autofocus: true,
              label: false,
              input_html: { autocomplete: "email", placeholder: "Email", class: "rounded-lg border z-0 w-full py-2 px-3 text-gray-700 focus:outline-none focus:shadow-outline"}%>
        <p class="text-xs text-gray-400 mb-5">We'll email you trip confirmations and receipts.</p>
        <%= f.input :password,
              required: true,
              label: false,
              input_html: { autocomplete: "new-password", placeholder: "Password", class: "rounded-lg border z-0 w-full py-2 px-3 mb-2 text-gray-700 focus:outline-none focus:shadow-outline" } %>
        <label class="rounded-lg border z-0 w-full bg-gray-100 py-2 px-3 inline-block focus:outline-none focus:shadow-outline text-center" style="color: #9EA6B1;">
          Upload a Profile Photo
          <span style="display:none;">
            <%= f.input :photo, as: :file, required: true %>
          </span>
        </label>
        <%= f.button :submit, "Sign Up", id: "submit", class: "rounded-lg border z-0 w-full py-2 px-3 mt-8 text-gray-700 focus:outline-none focus:shadow-outline"%>
        <%= f.error_notification %>
      </div>
    </div>
  <% end %>
</div>
<%# <%= render "devise/shared/links" %>
